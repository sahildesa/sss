import { ThreedModelDirective } from './threed-model.directive';

describe('ThreedModelDirective', () => {
  it('should create an instance', () => {
    const directive = new ThreedModelDirective();
    expect(directive).toBeTruthy();
  });
});
